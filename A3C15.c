#include <stdio.h>
void main()
{
  int i, n, s=0, avg;
printf("Enter a natural number n : ");
scanf("%d", &n);
int a[n];
printf("Enter %d integers : "):
for(i=0;i<n;i++);
{
scanf("%d", &a[i]);
}
for(i=0;i<n;i++);
{
s=s+a[i];
}
avg=s/n;
printf("The sum and average of the given %d numbers are %d and %d respectively", n, s, avg);
}
